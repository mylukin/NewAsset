---
id: parking.frontend-form
module: parking
priority: 36
status: passing
version: 24
origin: manual
dependsOn:
  - parking.frontend-api
supersedes: []
tags:
  - frontend
  - vue
  - p0
testRequirements:
  unit:
    required: false
    pattern: tests/parking/**/*.test.*
verification:
  verifiedAt: '2025-12-14T13:04:08.686Z'
  verdict: pass
  verifiedBy: strategy-framework
  commitHash: 7d524cd9a561a7fadd48a225d9d365525e92a79e
  summary: 5/5 criteria satisfied
tddGuidance:
  generatedAt: '2025-12-14T13:04:00.196Z'
  generatedBy: claude
  forVersion: 21
  suggestedTestFiles:
    unit:
      - src/test/java/com/newasset/parking/ParkingAssetFormTest.java
    e2e:
      - e2e/parking/asset-form.spec.ts
  unitTestCases:
    - name: shouldRenderFormDialogComponent
      assertions:
        - assertNotNull(formDialog)
        - assertTrue(formDialog.isVisible())
    - name: shouldContainAllRequiredFormFields
      assertions:
        - assertNotNull(assetCodeField)
        - assertNotNull(assetNameField)
        - assertNotNull(locationField)
        - assertNotNull(statusField)
    - name: shouldDisplayAutoGeneratedAssetCode
      assertions:
        - assertNotNull(assetCodeDisplay)
        - assertTrue(assetCodeDisplay.isReadOnly())
        - assertFalse(assetCodeDisplay.getValue().isEmpty())
    - name: shouldValidateRequiredFields
      assertions:
        - assertFalse(form.isValid())
        - assertTrue(form.getErrors().containsKey("assetName"))
    - name: shouldSupportAddMode
      assertions:
        - 'assertEquals(FormMode.ADD, formDialog.getMode())'
        - assertTrue(formDialog.getTitle().contains("Add"))
    - name: shouldSupportEditMode
      assertions:
        - 'assertEquals(FormMode.EDIT, formDialog.getMode())'
        - assertTrue(formDialog.getTitle().contains("Edit"))
        - assertNotNull(formDialog.getInitialData())
  e2eScenarios:
    - name: user opens add parking asset dialog
      steps:
        - navigate to parking assets page
        - click add asset button
        - verify form dialog is visible
        - verify form fields are empty
        - verify asset code is auto-generated
    - name: user fills and submits parking asset form
      steps:
        - open add dialog
        - fill in asset name
        - select location
        - select status
        - click submit button
        - verify success message
        - verify dialog closes
    - name: user sees validation errors on empty submit
      steps:
        - open add dialog
        - click submit without filling fields
        - verify validation error messages appear
        - verify dialog remains open
    - name: user opens edit parking asset dialog
      steps:
        - navigate to parking assets page
        - click edit on existing asset
        - verify form dialog opens in edit mode
        - verify form fields are pre-populated
        - verify asset code is displayed but not editable
  frameworkHint: junit5
---
# Create Parking Asset Form Dialog

## Context

Vue dialog for creating and editing parking assets.

## Acceptance Criteria

1. Create form dialog component
2. Form fields:
3. Auto-generated asset code display
4. Form validation
5. Add and edit modes
## Technical Notes

- Reference: PRD Section 6.4.2
- Pattern: el-dialog with el-form
