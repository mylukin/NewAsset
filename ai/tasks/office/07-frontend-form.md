---
id: office.frontend-form
module: office
priority: 54
status: passing
version: 23
origin: manual
dependsOn:
  - office.frontend-api
supersedes: []
tags:
  - frontend
  - vue
  - p0
testRequirements:
  unit:
    required: false
    pattern: tests/office/**/*.test.*
verification:
  verifiedAt: '2025-12-14T13:30:55.784Z'
  verdict: pass
  verifiedBy: strategy-framework
  commitHash: 0e85745029383b25eab11f76d3322c6f18cee10f
  summary: 5/5 criteria satisfied
tddGuidance:
  generatedAt: '2025-12-14T13:30:44.752Z'
  generatedBy: claude
  forVersion: 21
  suggestedTestFiles:
    unit:
      - src/test/java/com/example/asset/office/OfficeAssetFormDialogTest.java
    e2e:
      - e2e/office/office-asset-form.spec.ts
  unitTestCases:
    - name: shouldRenderFormDialogComponent
      assertions:
        - assertNotNull(formDialog)
        - assertTrue(formDialog.isVisible())
    - name: shouldDisplayAllRequiredFormFields
      assertions:
        - assertNotNull(nameField)
        - assertNotNull(officeTypeField)
        - assertNotNull(statusField)
        - assertNotNull(locationField)
    - name: shouldShowConditionalFieldsBasedOnOfficeType
      assertions:
        - 'assertEquals(expectedFields, getVisibleFieldsForType(''HEADQUARTERS''))'
        - 'assertEquals(expectedFields, getVisibleFieldsForType(''BRANCH''))'
    - name: shouldDisplayAutoGeneratedAssetCode
      assertions:
        - assertNotNull(assetCodeDisplay)
        - assertTrue(assetCodeDisplay.getText().matches("OFF-\\d+"))
    - name: shouldValidateRequiredFields
      assertions:
        - assertFalse(form.isValid())
        - assertTrue(getValidationErrors().contains("Name is required"))
  e2eScenarios:
    - name: user opens form dialog and sees all fields
      steps:
        - navigate to office assets page
        - click add asset button
        - verify form dialog is visible
        - verify all form fields are present
    - name: user sees conditional fields based on office type selection
      steps:
        - open form dialog
        - select HEADQUARTERS type
        - verify headquarters-specific fields appear
        - select BRANCH type
        - verify branch-specific fields appear
    - name: user sees auto-generated asset code
      steps:
        - open form dialog
        - verify asset code field is displayed
        - verify asset code follows expected format
    - name: user cannot submit form with invalid data
      steps:
        - open form dialog
        - leave required fields empty
        - click submit button
        - verify validation errors are shown
        - verify form is not submitted
    - name: user successfully submits form with valid data
      steps:
        - open form dialog
        - fill in all required fields
        - click submit button
        - verify success message
        - verify dialog closes
  frameworkHint: junit5
---
# Create Office Asset Form Dialog

## Context

Vue dialog for creating and editing office assets.

## Acceptance Criteria

1. Create form dialog component
2. Form fields:
3. Conditional field display based on officeType
4. Auto-generated asset code display
5. Form validation
## Technical Notes

- Reference: PRD Section 6.6.2
- Pattern: el-dialog with el-form
- Conditional: v-if for IT-specific fields
