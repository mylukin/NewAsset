---
id: facility.frontend-form
module: facility
priority: 26
status: passing
version: 24
origin: manual
dependsOn:
  - facility.frontend-api
supersedes: []
tags:
  - frontend
  - vue
  - p0
testRequirements:
  unit:
    required: false
    pattern: tests/facility/**/*.test.*
verification:
  verifiedAt: '2025-12-14T12:45:30.875Z'
  verdict: pass
  verifiedBy: strategy-framework
  commitHash: 7a350573c78cd18050a88d38796ec788e920f758
  summary: 5/5 criteria satisfied
tddGuidance:
  generatedAt: '2025-12-14T12:43:28.712Z'
  generatedBy: claude
  forVersion: 21
  suggestedTestFiles:
    unit:
      - src/test/java/com/example/facility/FacilityAssetFormTest.java
    e2e:
      - e2e/facility/facility-asset-form.spec.ts
  unitTestCases:
    - name: shouldRenderFormDialogComponent
      assertions:
        - assertNotNull(formDialog)
        - assertTrue(formDialog.isVisible())
    - name: shouldContainAllRequiredFormFields
      assertions:
        - assertNotNull(form.getField('name'))
        - assertNotNull(form.getField('type'))
        - assertNotNull(form.getField('location'))
        - assertNotNull(form.getField('status'))
    - name: shouldDisplayAutoGeneratedAssetCode
      assertions:
        - assertNotNull(form.getField('assetCode'))
        - assertTrue(form.getField('assetCode').isReadOnly())
        - assertFalse(form.getAssetCode().isEmpty())
    - name: shouldValidateRequiredFields
      assertions:
        - assertFalse(form.isValid())
        - assertTrue(form.getErrors().containsKey('name'))
        - 'assertEquals(''Name is required'', form.getErrors().get(''name''))'
    - name: shouldSupportAddMode
      assertions:
        - 'assertEquals(FormMode.ADD, formDialog.getMode())'
        - assertTrue(form.isEmpty())
        - 'assertEquals(''Add Facility Asset'', formDialog.getTitle())'
    - name: shouldSupportEditMode
      assertions:
        - 'assertEquals(FormMode.EDIT, formDialog.getMode())'
        - assertFalse(form.isEmpty())
        - 'assertEquals(''Edit Facility Asset'', formDialog.getTitle())'
  e2eScenarios:
    - name: user opens add facility asset dialog
      steps:
        - navigate to facility asset list page
        - click add asset button
        - verify dialog opens with empty form
        - verify title shows 'Add Facility Asset'
    - name: user fills and submits new facility asset form
      steps:
        - open add dialog
        - fill in name field
        - select asset type
        - enter location
        - select status
        - verify asset code is auto-generated
        - click save button
        - verify dialog closes
        - verify success notification
    - name: user opens edit facility asset dialog
      steps:
        - navigate to facility asset list page
        - click edit button on existing asset
        - verify dialog opens with pre-filled data
        - verify title shows 'Edit Facility Asset'
    - name: user sees validation errors on invalid submission
      steps:
        - open add dialog
        - leave required fields empty
        - click save button
        - verify validation error messages appear
        - verify dialog remains open
  frameworkHint: junit5
---
# Create Facility Asset Form Dialog

## Context

Vue dialog for creating and editing facility assets.

## Acceptance Criteria

1. Create form dialog component
2. Implement form fields:
3. Show auto-generated asset code
4. Add validation rules
5. Support add and edit modes
## Technical Notes

- Reference: PRD Section 6.3.2
- Pattern: el-dialog with el-form
