<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.asset.mapper.AssetParkingMapper">

    <resultMap type="com.ruoyi.asset.domain.entity.AssetParking" id="AssetParkingResult">
        <id property="assetId" column="asset_id"/>
        <result property="parkingNo" column="parking_no"/>
        <result property="parkingZone" column="parking_zone"/>
        <result property="parkingType" column="parking_type"/>
        <result property="area" column="area"/>
        <result property="currentUser" column="current_user"/>
        <result property="plateNo" column="plate_no"/>
        <result property="rentPrice" column="rent_price"/>
        <result property="contractNo" column="contract_no"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectAssetParkingVo">
        select asset_id, parking_no, parking_zone, parking_type, area, current_user,
               plate_no, rent_price, contract_no,
               create_by, create_time, update_by, update_time
        from t_asset_parking
    </sql>

    <select id="selectByAssetId" parameterType="Long" resultMap="AssetParkingResult">
        <include refid="selectAssetParkingVo"/>
        where asset_id = #{assetId}
    </select>

    <delete id="deleteByAssetId" parameterType="Long">
        delete from t_asset_parking
        where asset_id = #{assetId}
    </delete>

</mapper>
